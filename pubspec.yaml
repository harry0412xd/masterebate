name: masterebate
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '^3.0.0'
  flutter: '^3.10.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  # file_picker: ^8.1.2
  file_picker: ^10.3.8
  csv: ^6.0.0
  intl: ^0.20.2
  image_picker: ^1.0.7
  share_plus: ^12.0.1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
